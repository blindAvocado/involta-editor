Чтобы запустить проект, пропишите следующие команды:
`docker build -t app .`
`docker run --name involta-editor -d -p 1234:1234 involta-editor`

Для остановки контейнера пропишите:
`docker stop involta-editor`